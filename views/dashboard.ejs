<h1 class="text-center">Welcome  <%- user_info[0].user_name%></h1>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: #f0f4f9;
      min-height: 100vh;
    }
    .dashboard-title {
      text-align: center;
      font-weight: 700;
      font-size: 2rem;
      margin: 30px 0;
      color: #2c3e50;
    }
    .card {
      border: none;
      border-radius: 20px;
      padding: 20px;
      box-shadow: 0 6px 15px rgba(0,0,0,0.08);
      transition: transform 0.3s ease;
    }
    .card:hover {
      transform: translateY(-5px);
    }
    .pending {
      background: #fff6f2;
      border-left: 6px solid #ff7e5f;
    }
    .processing {
      background: #f2f9ff;
      border-left: 6px solid #4facfe;
    }
    .completed {
      background: #f5fff2;
      border-left: 6px solid #43e97b;
    }
    .card h5 {
      font-weight: 600;
      margin-bottom: 15px;
      color: #34495e;
    }
    .task-item {
      background: #fff;
      border: 1px solid #eee;
      border-radius: 8px;
      padding: 10px 12px;
      margin-bottom: 8px;
      font-size: 14px;
      font-weight: 500;
      color: #2c3e50;
    }
    .task-item:hover {
      background: #fafafa;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2 class="dashboard-title">📊 User Dashboard</h2>
    <div class="row g-4">
      
      <!-- Pending Tasks -->
      <div class="col-md-4">
        <div class="card pending">
          <h5>📌 Pending Tasks</h5>
         
        </div>
      </div>

      <!-- Processing Tasks -->
      <div class="col-md-4">
        <div class="card processing">
          <h5>⚙️ Processing Tasks</h5>
         
        </div>
      </div>

      <!-- Completed Tasks -->
      <div class="col-md-4">
        <div class="card completed">
          <h5>✅ Completed Tasks</h5>
        
        </div>
      </div>

    </div>
  </div>
</body>
</html>


 <!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #89f7fe, #66a6ff);
      min-height: 100vh;
    }
    .container {
      padding-top: 50px;
    }
    .card {
      border: none;
      border-radius: 15px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
    }
    .card h5 {
      font-weight: 600;
    }
    .pending {
      background: linear-gradient(135deg, #ffecd2, #fcb69f);
      color: #333;
    }
    .processing {
      background: linear-gradient(135deg, #a1c4fd, #c2e9fb);
      color: #333;
    }
    .completed {
      background: linear-gradient(135deg, #d4fc79, #96e6a1);
      color: #333;
    }
    .task-item {
      background: rgba(255,255,255,0.7);
      border-radius: 8px;
      padding: 8px 12px;
      margin-bottom: 8px;
      font-size: 14px;
      font-weight: 500;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2 class="text-center mb-5 text-white fw-bold">Dashboard</h2>
    <div class="row g-4">
      <!-- Pending Tasks -->
      <!-- <div class="col-md-4">
        <div class="card pending p-3">
          <h5 class="text-center mb-3">📌 Pending Tasks</h5>
          <div class="task-item">Task 1: Prepare docs</div>
          <div class="task-item">Task 2: Fix login bug</div>
          <div class="task-item">Task 3: Review design</div>
        </div>
      </div>

      <!-- Processing Tasks 
      <div class="col-md-4">
        <div class="card processing p-3">
          <h5 class="text-center mb-3">⚙️ Processing Tasks</h5>
          <div class="task-item">Task 4: Develop API</div>
          <div class="task-item">Task 5: DB Integration</div>
        </div>
      </div>

      Completed Tasks -->
      <!-- <div class="col-md-4">
        <div class="card completed p-3">
          <h5 class="text-center mb-3">✅ Completed Tasks</h5>
          <div class="task-item">Task 6: Setup project</div>
          <div class="task-item">Task 7: Create UI mockup</div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>  -->
