<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Home</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;700&display=swap" rel="stylesheet">
  <style>
    * {margin:0; padding:0; box-sizing:border-box;}
    body {
      font-family: 'Poppins', sans-serif;
      height:100vh;
      display:flex;
      justify-content:center;
      align-items:center;
      background: linear-gradient(135deg, #89f7fe, #66a6ff);
    }
    .container {
      background:#fff;
      border-radius:20px;
      box-shadow:0 12px 30px rgba(0,0,0,0.2);
      width:400px;
      padding:40px 30px;
      text-align:center;
    }
    .welcome-text {
      font-size:32px;
      font-weight:700;
      color:#333;
      margin-bottom:30px;
    }
    .btn {
      display:block;
      width:100%;
      padding:14px;
      margin:12px 0;
      border:none;
      border-radius:10px;
      font-size:18px;
      font-weight:600;
      cursor:pointer;
      transition:all 0.3s ease;
    }
    .btn-register {
      background: linear-gradient(135deg, #ff758c, #ff7eb3);
      color:#fff;
    }
    .btn-register:hover {
      transform:scale(1.05);
      background: linear-gradient(135deg, #ff5b77, #ff6aa2);
    }
    .btn-login {
      background: linear-gradient(135deg, #42e695, #3bb2b8);
      color:#fff;
    }
    .btn-login:hover {
      transform:scale(1.05);
      background: linear-gradient(135deg, #34c97a, #329a9d);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="welcome-text">Welcome</div>
    <form action="/register">
      <button class="btn btn-register">Register</button>
    </form>
    <form action="/login">
      <button class="btn btn-login">Login</button>
    </form>
  </div>
</body>
</html>
